{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for the app",
  "main": "app.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/server.js",
    "test": "test",
    "server": "npx nodemon ./src/server.ts",
    "deploy": "tsc -p tsconfig.json && node ./dist/server.js"
  },
  "author": "Gabriel Movila",
  "license": "ISC",
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/express": "^4.17.1",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "express": "^4.17.1",
    "mongoose": "^6.8.0"
  }
}
